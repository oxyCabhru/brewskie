services:
  brewskie:
    depends_on:
      - redis
    image: oxycabhru/brewskie
    container_name: brewskie
    restart: always
    ports:
      - 80:3000
  redis:
    image: redis
    ports:
      - 6379:6379
    volumes:
      - /var/redis/brewskie:/data:rw
